 <div class="index-header-image">
  <nav class="navbar navbar-default" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="container">
        <div class="navbar-brand"><%= link_to "BRAND", root_path %></div>
      </div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <div class="container">
        <ul class="nav navbar-nav pull-right">
          <% if user_signed_in? %>
            <li><%= link_to "View Profile", profiles_path %></li>
            <li><%= link_to "Edit Profile", edit_profile_path(2) %></li>
            <li><%= link_to "Settings", root_path %></li>
          <% else %>
          <li><%= link_to "Home", root_path %></li>
          <li><a href="#">For Employers</a></li>
          <li><a href="#">Recommend</a></li>
          <li><a href="#">FAQ</a></li>
          <% end %> 
          <% if user_signed_in? %>
            <li><%= link_to "Sign Out", destroy_user_session_path, method: :delete %></li>
             <% else %>
          <li><%= link_to "Sign In", new_user_session_path %></li>
          <li><button type="button" class="btn btn-small btn-success"><%= link_to "Apply", new_user_registration_path %></button></li>
          <% end %>
        </ul>
      </div>
    </div><!-- /.navbar-collapse -->
  </nav>

